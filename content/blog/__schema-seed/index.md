---
title: Schema seed (do not delete)
date: 2020-01-01T00:00:00.000Z
description: Hidden post — gives gatsby-transformer-remark a frontmatter shape to infer. Filtered out everywhere via `private: true`.
private: true
---

This post never renders. Its only purpose is to give `gatsby-transformer-remark` a non-empty markdown source so it can infer the frontmatter schema (including the `private` boolean field used to filter posts).

Delete this file as soon as you publish a real post.
